# xBadge

内联状态徽标，四种语义变体。全局注册为 `<Badge />`。

**Markdown 中使用：**
```html
<Badge text="新增" type="tip" />
<Badge text="已废弃" type="warning" />
```

**类型：** `info` | `tip` | `warning` | `danger`

颜色固定，不随主题色变化。
